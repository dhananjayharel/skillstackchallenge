import { SearchenvironmentpipePipe } from './searchenvironmentpipe.pipe';

describe('SearchenvironmentpipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchenvironmentpipePipe();
    expect(pipe).toBeTruthy();
  });
});
